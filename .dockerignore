# Never bake secrets into the image
.env
.env.*
# Build artefacts recreated inside the container
node_modules
dist
# Runtime data (mounted as a volume)
data
# Dev / VCS
.git
.github
.gitignore
*.log
# Editor cruft
.idea
.vscode
*.swp
